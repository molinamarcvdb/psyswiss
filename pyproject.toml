[project]
name = "psy-swiss"
version = "0.1.0"
description = "Swiss psychology jobs tracker + EU-to-psychotherapist licensing guide (info only)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi==0.141.1",
    "uvicorn[standard]==0.52.4",
    "jinja2==3.1.6",
    "apscheduler==3.11.3",
    "scrapling[fetchers]==0.4.15",
    "httpx==0.28.1",
]

[tool.uv]
package = false
